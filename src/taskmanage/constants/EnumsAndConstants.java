package taskmanage.constants;
public class EnumsAndConstants {

    // Enum for Task Status
    public enum TaskStatus {
        COMPLETE,
        PENDING,
        OVERDUE
    }

    // Enum for Priority Levels
    public enum PriorityLevel {
        LOW,
        MEDIUM,
        HIGH
    }
}
